# Конструктор програми харчування — нотатки

**Продакшн:** GitHub Pages — репозиторій `igsamchenko-cmyk/konstruktor-harchuvannya`, сайт `https://igsamchenko-cmyk.github.io/konstruktor-harchuvannya/`.
**Файли:** `index.html` (уся логіка), `manifest.webmanifest`, `sw.js`, `icon-192.png`, `icon-512.png`, `training-constructor-preview.jpg`, `training-exercise-preview.webp`, `i18n-*.js` (PWA, колаж і повний UA/EN).
**Версія 10.13, оновлено 27.08.2026.** Поточний офлайн-кеш у `sw.js` — `nutri-konstruktor-v27`; при наступній зміні `index.html` підвищити його до `v28`.

## Як влаштовано
- Розрахунок цілей: Міффлін–Сан Жеор → TDEE (1.20–1.75) → корекція під ціль **за темпом зміни ваги** (кг/тиждень × 7700 ккал/кг). Мінімум: 1200 ж / 1500 ч. Вода 30–35 мл/кг.
- БЖВ: білок 1.2–2.4 г/кг **розрахункової ваги** (1.8), жири 20–35% з нижньою межею, вуглеводи — залишок, клітковина 14 г/1000 ккал. Формули перевірені: `prot*4+fat*9+carb*4 ≈ kcal`.
- Прийоми: 3/4/5 + опційний «Смаколик» (0–20%). План — `getPlan()`, час — `mealTimes()` (редагований).
- **Підбір грамів (ядро)** — координатний спуск, див. розділ нижче.
- Функції: генерація тижня/дня, **режими тижня**, звичайний/компактний об’єм їжі, копіювання дня, ручні грами з живим перерахунком, **мотиваційні підказки балансу БЖВ/клітковини**, орієнтовний світлофор натрію/насичених жирів/вільних цукрів, виключення продуктів, покупки, друк/PDF, .json, **два типи посилань**, **клієнтський режим із швидкими рівноцінними замінами**.

## Клінічна частина (v10) — КЛЮЧОВЕ
Що було не так у v9 і як виправлено:

| Проблема v9 | Рішення v10 |
|---|---|
| Білок рахувався від фактичної ваги: 1.8 г/кг на 120 кг = білок на жирову тканину | `calcWeight()` — adjusted body weight: вага за ІМТ 25 + чверть надлишку. Виняток: якщо спеціаліст позначив виражену м’язову масу, використовується фактична вага |
| Жири могли впасти до 27 г (1200 ккал × 20%) | `fatMin = max(40 г, 0.6 г/кг розрахункової ваги)`; при спрацюванні — попередження в `renderMacroWarn` |
| Клітковина не рахувалась узагалі | Поле `fb` у 153 продуктах, ціль `t.fib`, вага `OPT_W.fb` в оптимізаторі + **асиметричний штраф** у `dayScore` (недобір карається в 10× сильніше за перебір), плитка й рядок у зведенні дня, колонка в PDF |
| Дефіцит у відсотках однаково для 55 і 110 кг | Повзунки темпу: схуднення 0.2–1.0 кг/тиж (за замовч. 0.35), набір 0.1–0.4 (0.2). `paceInfo()` рахує відсоток назад. Прогноз ваги на 4/8/12 тижнів, попередження при темпі > 1% маси тіла |
| ІМТ помилково називав м’язистих клієнтів людьми з надлишковою вагою | ІМТ показується як скринінг без діагностичного ярлика; додано `fMuscular`, співвідношення талії до зросту та пояснення. Для атлетичної статури розрахункова вага враховує 50% перевищення над вагою за ІМТ 25, але не всю фактичну вагу |
| Білок на прийом ніде не перевірявся | Окремий пул `snackProtein`, нижні орієнтири для основних прийомів і перекусів у `mealProteinPenalty()`, візуальна плашка `.mprot` та рядок розподілу під зведенням |
| Смаколики 20% = ~400 ккал цукру без застережень | Попередження про рекомендацію ВООЗ (< 10% енергії) при слайдері > 10% |
| Порції на межі здорового глузду | `gb:[lo,hi]` — ручні межі для 30 продуктів; цибуля → `season`; `SCALABLE` (стеля росте лише для круп/м'яса/страв/молочного); абсолютна стеля 500 г. Прибрало «350 г зеленої цибулі» і «555 г айрану» |
| Приправи потрапляли у випадаючі списки | `selectList()` фільтрує `season` (крім уже обраного) |

**Незалежний регресійний прогін (4200 днів, 8 профілів × 5 фіксованих генераторів):** калорії, білок і жири — 100% днів у ±10%; вуглеводи — 99,76%; клітковина не нижче 90% цілі у 99,93% днів. Максимальна порція — 500 г, приправи не обираються автоматично. Швидкодія: тиждень ≈ 23 мс.

## Підбір грамів під КБЖВ (ядро)
1. `buildDay(dayComps, t)` — стартові грами за калорійними частками.
2. `optimizeDay(meals, t)` — координатний спуск: точний 1-D мінімум зваженої квадратичної помилки за **5 цілями** (K/P/F/C/**Fb**, ваги `OPT_W = {k:3.0, p:2.6, f:2.6, c:2.0, fb:0.3}`) + збереження калорійності прийому (`OPT_WM`) + тяжіння до стартових грамів (`OPT_LAM`). 24 проходи, `customG` не чіпаються, `gramBounds` дотримуються.
3. `adaptShares(mealId, t)` — частки компонентів масштабуються під цільовий розподіл БЖВ (`BASE_SPLIT` = 30/27/43).
4. `PORTION_SCALE = clamp(kcal/2000, 1, 1.8)`, застосовується лише до `SCALABLE`-категорій, з абсолютною стелею 500 г.
5. `genDay` генерує до 40 варіантів і лишає найкращий за `dayScore` (рання зупинка при score < 0.002). Збільшення з 20 до 40 усунуло рідкісні відхилення білка й жирів понад 10% у регресійному прогоні.

**Ваги c=2.0 / fb=0.3 підібрані експериментально:** при fb=0.5 клітковина перетягувала грами на овочі й вуглеводи недобирали −8%; при fb=0.3 недобір впав до −2%, а клітковина все одно перевиконується.

## Реальність кухні (v10)
- **Режими тижня** (`state.settings.weekMode`, селект у розширених): `vary` — 7 різних днів; `rot2`/`rot3` — 2 або 3 меню чергуються; `batch` — `applyBatch()` копіює вечерю дня N в обід дня N+1 (обід і вечеря мають однакові категорії, тож ті самі продукти лише перераховуються під іншу частку). У шапці обіду з'являється «вчорашня вечеря, лише розігріти».
- **Сира / суха вага:** прапорець `raw` у 25 позицій (м'ясо, риба, субпродукти, фарші — але не консерви й не запечена скумбрія). Підпис «г сир.» / «г сух.», пояснювальний блок під конструктором і рядок у підвалі PDF.
- **Зручні побутові порції:** `snapUnitGrams()` фіксує яйця, хлібці, скибки й дрібні вироби цілими одиницями, а ложки та великі десерти — половинами; після цього решта продуктів повторно оптимізується. `unitApprox()` показує природні дроби (`½`, `1½`), покупки округлює до цілих одиниць — «Яйця курячі 385 г (7 шт.)». Однаково в UI та PDF.
- **Рецепти:** поле `rc` у всіх 18 стравах — склад із вагами й вихід. Показується під компонентом (`.rcnote`), у PDF — додаток «Як готувати страви з меню» (лише для страв, що є в тижні; в автогенерацію страви не потрапляють, тож розділ з'являється після ручного вибору).
- **Компактний об’єм:** `foodVolume:'compact'` зменшує верхні межі порцій овочів і некалорійних фруктів та послаблює тяжіння оптимізатора до надлишкової клітковини. Калорії, білок і жири залишаються під тим самим регресійним контролем.

## Якість раціону й клієнтські заміни (v10.12)
- `qualityNutrients()` дає орієнтовну оцінку натрію, насичених жирів і вільних цукрів для кожного продукту. У підсумку дня є триколірний індикатор відносно орієнтирів ВООЗ: натрій < 2000 мг, насичені жири та вільні цукри < 10% енергії.
- Це **скринінг**, а не точний лабораторний розрахунок: натрій готової страви залежить від рецепта, бренду й доданої солі. Інтерфейс прямо показує це застереження.
- У клієнтському режимі `swapAlternatives()` пропонує три близькі за білками, жирами та вуглеводами продукти, а `applyClientSwap()` одразу підставляє калорійно рівноцінну побутову порцію.
- Прогноз ваги показує коридор, а не одну гарантовану цифру: одна межа лінійна, друга враховує поступове зниження енерговитрат.

## Адаптивний зворотний зв’язок (v10.13)
- Трекер підтримує 3–7 зважувань на тиждень і рахує 7-денне ковзне середнє. Фактичний темп визначається робастною оцінкою Theil–Sen, щоб один стрибок води менше впливав на висновок.
- Висновок не з’являється раніше 3 записів/14 днів, а калорійний орієнтир — раніше 4 записів/21 дня. Крок обмежено 100–150 ккал/день; застосунок **ніколи не змінює калорійність автоматично**.
- Якщо подальше зниження калорій суперечило б сигналу нижче BMR, захисній межі або медичному скринінгу, числова порада блокується й замінюється рекомендацією перегляду зі спеціалістом.
- На графіку показані сирі зважування, 7-денний тренд і очікуваний коридор. До запису можна додати талію; вона відображається другою пунктирною лінією з незалежною шкалою.
- Прогноз навмисно динамічний: проста стала «7700 ккал = 1 кг» використовується лише для малого кроку корекції, а не для обіцянки кінцевої ваги. Методологічна основа: [Hall et al., Lancet 2011](https://pubmed.ncbi.nlm.nih.gov/21872751/), [NIDDK Body Weight Planner](https://www.niddk.nih.gov/bwp), [Hall, 2010](https://pubmed.ncbi.nlm.nih.gov/19934407/).

## Два режими (v10)
`state.ui.mode` = `pro` | `client`; `body[data-mode]` + класи `.pro-only` / `.client-only`.
- **Клієнтський режим:** анкета прихована, немає розширених налаштувань, кнопок генерації, виключень і «додати продукт». Продукти — текстом замість селектів, грами — тільки для читання. Чек-бокси «з'їдено» (`state.done`, ключ `день:прийом`) і лічильник прогресу. Зведення дня без бейджів відхилення. Заголовок стає «Програма харчування — Ім'я».
- **Нотатка спеціаліста** (`state.note`, textarea в кроці 4) видно клієнту над меню й у PDF.
- **Два посилання:** «👤 Посилання клієнту» (`m:'c'` у payload → `ui.locked = true`, кнопка режиму прихована) і «🔗 Посилання колезі» (повний доступ). Формат `#p=` — v3, deflate-raw + base64url, ≈880 символів.
- З клієнтського вигляду є непомітний вихід унизу: «Я спеціаліст — відкрити налаштування» (з підтвердженням).

## База продуктів: 233 позиції, 8 продуктових категорій
До білкового пулу додано твердий і шовковий тофу, темпе, сейтан, едамаме, соєвий йогурт і напій; бобові мають додаткову роль `protein` без дублювання записів. `snackProtein` — службова роль, а не окрема категорія покупок.
Внутрішня узгодженість: середнє відхилення (4p+9f+4c) від заявлених ккал = −0.8%.
Прапорці: `dry`, `raw`, `brk`, `oil`, `add`, `season`, `dish`, `u`/`un`, `custom`, `fb` (клітковина), `gb` (ручні межі порції), `rc` (рецепт).

## Історія версій (стисло)
- **v2**: PWA + офлайн; localStorage (`nutri_konstruktor_v1`); трекінг ваги; CTA (Telegram `@samchenko_i`).
- **v3**: темна тема; hero-результат + смуга БЖВ; мобільні картки; фірмовий PDF.
- **v4**: смаколики 0–20%, `syncSweetSlot()`.
- **v5**: рамки карток, контраст калорій, чипи категорій.
- **v6**: база 115→217 (традиційні українські продукти); страви; уточнені `gramBounds`.
- **v7**: страви розкидані по природних категоріях групою «Страви (≈)».
- **v8**: виключення продуктів; людські порції (`u/un`); «Поділитися посиланням»; вода, час прийомів.
- **v9** (21.08): виправлено розрахунок БЖВ (координатний спуск + `adaptShares`); видимі попередження про відхилення.
- **v10** (25.08): **клінічна ревізія** (розрахункова вага, межа жирів, темп замість відсотка, клітковина, ІМТ/талія, білок на прийом), **реальність кухні** (режими тижня, сира/суха вага, покупки в штуках, рецепти), **клієнтський режим** і два типи посилань. Виправлено: чек-бокс чіпляв обробник зміни продукту; `fTraining` не відновлювався зі збереження («0.1» ≠ «0.10») — тепер `setSelectNum()` бере найближчу опцію.
- **v10.1** (26.08): коректна інтерпретація ІМТ за вираженої м’язової маси; додано співвідношення талії до зросту (<0,5 / 0,5–0,59 / ≥0,6).
- **v10.2** (26.08): адаптивний колаж переваг конструктора тренувань (персоналізація, прогресія, техніка) та повний перемикач UA/EN із перекладом 217 продуктів, 18 рецептів, динамічних розрахунків, клієнтського режиму й PDF. Мова зберігається локально.
- **v10.3** (26.08): ручне редагування грамів у меню стало живим — після введення ваги додаток фіксує цей рядок, перераховує інші порції, шапку прийому, підсумки дня та покупки. У смаколики додано 10 популярних позицій: Наполеон, Спартак, Прага, сметанник, еклер, трубочка зі згущеним молоком, пахлава, макарон, донат і тістечко «Картопля».
- **v10.4** (26.08): у підсумку дня додано блок «Підказки балансу» для спеціаліста: він показує, чого бракує або що в надлишку за білками, жирами, вуглеводами й клітковиною, та дає коротку практичну дію без демотивуючого тону.
- **v10.5** (26.08): діагностика аномальної калорійності показала, що `<input max>` не захищав формулу від ручного введення віку поза діапазоном (наприклад, 339 років → BMR ≈ 248 ккал і спрацювання мінімуму 1500 ккал). Додано `sanitizeProfile()` і спільний `bindProfileNumber()` для віку, зросту, ваги й талії: розрахунок завжди використовує допустимі межі, а поле нормалізується при втраті фокуса.
- **v10.6** (26.08): легкий тематичний редизайн Clean Coach: оновлена зелено-макро палітра, тонкий CSS-pattern фону, макро-смуга в шапці, компактніші 8px панелі, кращі фокуси/кнопки/плитки/меню, оновлені PWA theme/background colors без додавання важких графічних assets.
- **v10.7** (26.08): розширені налаштування розділено за відповідальністю. Клієнт у своєму режимі може змінювати темп відповідно до цілі, кількість і час прийомів, частку смаколиків та режим тижня; професійні коефіцієнти білка й жирів лишаються прихованими. Додано повернення рекомендованих побутових значень без скидання професійних норм.
- **v10.8** (26.08): автопорції узгоджено з реальними кухонними одиницями — більше немає рекомендацій на кшталт `1,5 яйця`. Цілі продукти округлюються до штук/скибок, подільні міри — до половин із записом `½`; після округлення день повторно балансується. Аудит: усі 227 продуктів примусово перевірені в сумісних слотах (0 помилок), 1120 згенерованих днів дали 0 незручних побутових дробів, 100% днів у ±10% за калоріями, білком і жирами, 99,29% — за вуглеводами.
- **v10.9** (26.08): дорослий режим 18+, медичний скринінг із блокуванням автогенерації та клієнтського посилання, межі схуднення до 1% маси/тиждень і 25% TDEE, окремий сигнал нижче BMR без хибної жорсткої межі, білок не більше 35% енергії, пом’якшена атлетична розрахункова вага, типи харчування та безпечний веганський пул. Повністю прибрано останній fallback, який міг повернути виключений продукт. Перекуси отримали білковий слот, вечеря — меншу частку білка.
- **v10.10** (27.08): активні пресети виключень, ручно виключені продукти й точково повернені продукти зберігаються окремо. Пересічні набори більше не вмикають один одного помилково, вимкнення пресета прибирає саме його обмеження, а старі локальні збереження мігрують автоматично.
- **v10.11** (27.08): рядки продуктів переведено на стабільну шестиколонкову сітку. Поля ваги, одиниці та калорійність тепер мають однакові позиції незалежно від наявності побутової міри; мобільна картка зберігає читабельне двоколонкове розташування.
- **v10.12** (27.08): додано орієнтовний світлофор натрію, насичених жирів і вільних цукрів; три рівноцінні швидкі заміни для клієнта; коридор прогнозу ваги; звичайний/компактний об’єм їжі; повний UA/EN для нових елементів і оновлений PWA-кеш.
- **v10.13** (27.08): адаптивний трекер із 7-денним згладжуванням, робастним фактичним темпом, очікуваним коридором, відкладеною рекомендацією 100–150 ккал і запобіжниками; необов’язкова талія як друга лінія графіка; повний UA/EN та міграція старих записів.

## Постійний тест безпеки
`node tests/safety.test.cjs` перевіряє вікову межу, дефіцит, атлетичну розрахункову вагу, медичне блокування, недоторканність виключень, веганський склад, показники якості, клієнтські заміни, прогноз, компактний режим і адаптивні сценарії прогресу. Окремо перевіряються: нормальний/повільний темп, замалий період спостереження, талія та блокування глибшого дефіциту нижче BMR. Розширений відтворюваний прогін на 1260 днях: 99,60% днів одночасно в ±10% за калоріями/білком/жирами, 99,29% — усі основні прийоми мають ≥18 г білка, 99,68% — усі перекуси мають ≥10 г. Команда: `$env:NUTRI_TEST_WEEKS=30; node tests/safety.test.cjs`.

## Міграція зі старих збережень
`migrateSettings()` викликається після `loadLocal()`, `loadFromFile()`, `unpackStateFromHash()` і «Новий клієнт»: рахує `lossRate`/`gainRate` зі старих `deficit`/`surplus` через TDEE, підставляє `weekMode`, `foodVolume`, `times`, `ui`, `note`, `done`, а старі записи ваги нормалізує до нового формату з необов’язковою талією. Старий одиничний `activity` мігрує в `lifestyle`+`training` як раніше. Перевірено на збереженні v9 — анкета, виключення й записи ваги відновлюються.

## Монетизація
Додаток = безкоштовний лід-магніт + продаж персональних програм і супроводу через Telegram. Клієнтське посилання з v10 — уже готовий продукт, а не демо-версія інструмента: клієнт бачить меню й чек-лист, а не механіку розрахунку. SaaS-кабінет для тренерів (Supabase + LiqPay/WayForPay) — етап 3.

## Контроль доступу до програм тренувань
- Промокартка тренувань є лише демонстрацією: скриншот не клікабельний.
- На ній немає URL тренувального конструктора; доступ запитується виключно через Telegram або e-mail Ihor Samchenko.
- Це правило не замінює серверну авторизацію самого тренувального застосунку: публічний GitHub Pages потрібно вимкнути або захистити окремо.
## Ідеї для наступних версій
- Наступний рівень адаптації: короткий щотижневий check-in дотримання, голоду, енергії та тренувальної продуктивності, щоб відрізняти реальну потребу в корекції від пропусків меню.
- Конструктор тренувань (групи м'язів × рівень × інвентар).
- Окремі шаблони для тренувальних/нетренувальних днів.
- Замінити орієнтовну модель натрію/насичених жирів/вільних цукрів на повні перевірені значення для кожного бренду або рецепта, якщо з’явиться надійне джерело даних.
- Дефіцит вітамінів/мінералів — принаймні залізо, кальцій, B12 для вегетаріанських пресетів.
